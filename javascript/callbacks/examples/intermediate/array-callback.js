// ======================================
// ARRAY CALLBACK
// ======================================

// Numbers array
const numbers = [1, 2, 3, 4, 5];

// forEach callback
numbers.forEach((number) => {
  console.log(number * 2);
});
