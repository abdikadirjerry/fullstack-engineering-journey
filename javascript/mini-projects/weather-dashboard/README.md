# Modern Modular Weather Dashboard

A lightweight, clean weather tracking application configured natively over an ES-module design layout pattern.

## Highlights

- Separates data handling (`api.js`), local data tracking (`storage.js`), and user components into explicit files.
- Stores modern user choices natively across sessions.
